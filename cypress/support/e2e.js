// cypress/support/e2e.js
// Minimal Cypress support file required by Cypress 13+ test runner.
// Intentionally minimal so it doesn't mask real failures in screenshots/video.

beforeEach(() => {
  // Reset state between tests if needed.
});

afterEach(() => {
  // Global cleanup hook.
});
